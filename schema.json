{
    "input_source_map": {
        "name": "输入源→输出标签映射",
        "type": "object",
        "default": {
            "qq_text": "qchat",
            "qq_voice": "qchat voice=true",
            "desktop_speech": "speak",
            "desktop_text": "qchat"
        },
        "hint": "输入源到输出标签的映射。键为输入源类型，值为要补全的标签名"
    },
    "auto_detect": {
        "name": "启用自动检测",
        "type": "boolean",
        "default": true,
        "hint": "是否自动从事件上下文中检测输入源类型"
    },
    "force_patch": {
        "name": "强制补全模式",
        "type": "boolean",
        "default": true,
        "hint": "如果AI回复没有正确的输出标签，是否强制补全"
    },
    "debug_log": {
        "name": "调试日志",
        "type": "boolean",
        "default": false,
        "hint": "是否输出详细的调试日志"
    }
}